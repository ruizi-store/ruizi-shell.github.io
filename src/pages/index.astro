---

---

<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Ruizi Shell - 现代化跨平台终端工具，集成 SSH、SFTP、串口调试、VNC远程桌面、端口转发、AI助手等功能" />
    <title>Ruizi Shell - 现代化跨平台终端工具</title>
  </head>
  <body>
    <!-- Hero Section -->
    <section class="hero">
      <div class="container">
        <div class="hero-content">
          <div class="logo">
            <span class="logo-icon">⌘</span>
            <h1>Ruizi Shell</h1>
          </div>
          <p class="tagline">现代化的跨平台终端工具</p>
          <p class="description">
            集成 SSH、SFTP、串口调试、VNC远程桌面、端口转发、AI助手等功能于一体的终端应用，
            为开发者和运维人员打造的高效工作利器。
          </p>
          <div class="cta-buttons">
            <a href="#download" class="btn btn-primary">立即下载</a>
            <a href="#features" class="btn btn-secondary">了解更多</a>
          </div>
          <p class="free-badge">🎉 完全免费，无任何功能限制</p>
        </div>
        <div class="hero-image">
          <div class="terminal-preview">
            <div class="terminal-header">
              <span class="dot red"></span>
              <span class="dot yellow"></span>
              <span class="dot green"></span>
              <span class="terminal-title">ruizi-shell</span>
            </div>
            <div class="terminal-body">
              <p><span class="prompt">user@server</span>:<span class="path">~</span>$ ssh root@192.168.1.100</p>
              <p><span class="success">Connected to server</span></p>
              <p><span class="prompt">root@server</span>:<span class="path">~</span>$ _</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section id="features" class="features">
      <div class="container">
        <h2 class="section-title">功能特性</h2>
        <div class="features-grid">
          <div class="feature-card">
            <div class="feature-icon">💻</div>
            <h3>SSH 终端</h3>
            <p>支持密码、密钥认证，跳板机连接，X11 转发，多标签会话管理</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">📁</div>
            <h3>SFTP 文件管理</h3>
            <p>双面板文件管理器，支持拖拽上传下载，书签管理</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🔌</div>
            <h3>串口调试</h3>
            <p>支持多种波特率和串口参数配置，适合嵌入式开发调试</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🖥️</div>
            <h3>VNC 远程桌面</h3>
            <p>内置 VNC 客户端，支持多种编码格式，流畅的远程桌面体验</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🔗</div>
            <h3>端口转发</h3>
            <p>支持本地转发、远程转发、动态转发（SOCKS5 代理）</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🤖</div>
            <h3>AI 助手</h3>
            <p>支持接入 DeepSeek、OpenAI、Claude 等大模型，智能生成命令</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Download Section -->
    <section id="download" class="download">
      <div class="container">
        <h2 class="section-title">下载安装</h2>
        <div class="download-grid">
          <a href="./downloads/ruizi-shell-windows-x64.exe" class="download-card">
            <div class="os-icon">🪟</div>
            <h3>Windows</h3>
            <p>Windows 10/11 (64-bit)</p>
            <span class="download-btn">下载 .exe</span>
          </a>
          <a href="./downloads/ruizi-shell-macos-arm64.dmg" class="download-card">
            <div class="os-icon">🍎</div>
            <h3>macOS</h3>
            <p>macOS 11+ (Apple Silicon)</p>
            <span class="download-btn">下载 .dmg</span>
          </a>
          <a href="./downloads/ruizi-shell-linux-x64.AppImage" class="download-card">
            <div class="os-icon">🐧</div>
            <h3>Linux</h3>
            <p>Ubuntu, Debian, Fedora...</p>
            <span class="download-btn">下载 .AppImage</span>
          </a>
        </div>
        <p class="download-note">
          当前版本：<strong>v1.0.0</strong>
        </p>
      </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="contact">
      <div class="container">
        <h2 class="section-title">联系我们</h2>
        <div class="contact-grid">
          <div class="contact-card">
            <div class="contact-icon">📧</div>
            <h3>邮箱</h3>
            <a href="mailto:duanjunzi@gmail.com">duanjunzi@gmail.com</a>
          </div>
          <div class="contact-card">
            <div class="contact-icon">💬</div>
            <h3>QQ 群</h3>
            <div class="qr-placeholder">
              <span>二维码占位</span>
            </div>
            <p>群号：123456789</p>
          </div>
          <div class="contact-card">
            <div class="contact-icon">📱</div>
            <h3>微信群</h3>
            <div class="qr-placeholder">
              <span>二维码占位</span>
            </div>
            <p>群号：123456789</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
      <div class="container">
        <p>&copy; 2026 Ruizi Shell. All rights reserved.</p>
        <p class="footer-links">
          <a href="#features">功能特性</a> |
          <a href="#download">下载</a> |
          <a href="#contact">联系我们</a>
        </p>
      </div>
    </footer>
  </body>
</html>

<style is:global>
  :root {
    --primary: #3b82f6;
    --primary-dark: #2563eb;
    --bg-dark: #0f172a;
    --bg-card: #1e293b;
    --text: #f8fafc;
    --text-muted: #94a3b8;
    --border: #334155;
    --success: #22c55e;
    --accent: #8b5cf6;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    background: var(--bg-dark);
    color: var(--text);
    line-height: 1.6;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }

  /* Hero Section */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 80px 0;
    background: linear-gradient(135deg, var(--bg-dark) 0%, #1a1a2e 100%);
  }

  .hero .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 16px;
    margin-bottom: 16px;
  }

  .logo-icon {
    font-size: 48px;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .logo h1 {
    font-size: 48px;
    font-weight: 700;
    background: linear-gradient(135deg, var(--text), var(--primary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .tagline {
    font-size: 24px;
    color: var(--primary);
    margin-bottom: 16px;
  }

  .description {
    font-size: 18px;
    color: var(--text-muted);
    margin-bottom: 32px;
    max-width: 500px;
  }

  .cta-buttons {
    display: flex;
    gap: 16px;
    margin-bottom: 24px;
  }

  .btn {
    padding: 14px 32px;
    border-radius: 8px;
    font-size: 16px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .btn-primary {
    background: var(--primary);
    color: white;
  }

  .btn-primary:hover {
    background: var(--primary-dark);
    transform: translateY(-2px);
  }

  .btn-secondary {
    background: transparent;
    color: var(--text);
    border: 2px solid var(--border);
  }

  .btn-secondary:hover {
    border-color: var(--primary);
    color: var(--primary);
  }

  .free-badge {
    color: var(--success);
    font-size: 16px;
    font-weight: 500;
  }

  /* Terminal Preview */
  .terminal-preview {
    background: #1a1a2e;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
    border: 1px solid var(--border);
  }

  .terminal-header {
    background: #2d2d44;
    padding: 12px 16px;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }

  .dot.red { background: #ff5f56; }
  .dot.yellow { background: #ffbd2e; }
  .dot.green { background: #27ca40; }

  .terminal-title {
    margin-left: auto;
    color: var(--text-muted);
    font-size: 13px;
  }

  .terminal-body {
    padding: 20px;
    font-family: 'JetBrains Mono', 'Fira Code', monospace;
    font-size: 14px;
    line-height: 1.8;
  }

  .prompt { color: #22c55e; }
  .path { color: #3b82f6; }
  .success { color: #22c55e; }

  /* Features Section */
  .features {
    padding: 100px 0;
    background: var(--bg-dark);
  }

  .section-title {
    text-align: center;
    font-size: 36px;
    margin-bottom: 60px;
    background: linear-gradient(135deg, var(--text), var(--primary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
  }

  .feature-card {
    background: var(--bg-card);
    padding: 32px;
    border-radius: 16px;
    border: 1px solid var(--border);
    transition: all 0.3s ease;
  }

  .feature-card:hover {
    transform: translateY(-5px);
    border-color: var(--primary);
  }

  .feature-icon {
    font-size: 48px;
    margin-bottom: 16px;
  }

  .feature-card h3 {
    font-size: 20px;
    margin-bottom: 12px;
  }

  .feature-card p {
    color: var(--text-muted);
    font-size: 15px;
  }

  /* Download Section */
  .download {
    padding: 100px 0;
    background: linear-gradient(135deg, #1a1a2e 0%, var(--bg-dark) 100%);
  }

  .download-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
    margin-bottom: 40px;
  }

  .download-card {
    background: var(--bg-card);
    padding: 40px 32px;
    border-radius: 16px;
    border: 1px solid var(--border);
    text-align: center;
    text-decoration: none;
    color: var(--text);
    transition: all 0.3s ease;
  }

  .download-card:hover {
    transform: translateY(-5px);
    border-color: var(--primary);
  }

  .os-icon {
    font-size: 56px;
    margin-bottom: 16px;
  }

  .download-card h3 {
    font-size: 24px;
    margin-bottom: 8px;
  }

  .download-card p {
    color: var(--text-muted);
    margin-bottom: 20px;
  }

  .download-btn {
    display: inline-block;
    background: var(--primary);
    color: white;
    padding: 10px 24px;
    border-radius: 8px;
    font-weight: 500;
  }

  .download-note {
    text-align: center;
    color: var(--text-muted);
  }

  .download-note a {
    color: var(--primary);
  }

  /* Contact Section */
  .contact {
    padding: 100px 0;
    background: var(--bg-dark);
  }

  .contact-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
  }

  .contact-card {
    background: var(--bg-card);
    padding: 40px 32px;
    border-radius: 16px;
    border: 1px solid var(--border);
    text-align: center;
  }

  .contact-icon {
    font-size: 48px;
    margin-bottom: 16px;
  }

  .contact-card h3 {
    font-size: 20px;
    margin-bottom: 16px;
  }

  .contact-card a {
    color: var(--primary);
    text-decoration: none;
  }

  .contact-card a:hover {
    text-decoration: underline;
  }

  .qr-placeholder {
    width: 150px;
    height: 150px;
    background: var(--border);
    border-radius: 8px;
    margin: 0 auto 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-muted);
    font-size: 14px;
  }

  /* Footer */
  .footer {
    padding: 40px 0;
    background: var(--bg-card);
    text-align: center;
    border-top: 1px solid var(--border);
  }

  .footer p {
    color: var(--text-muted);
    margin-bottom: 12px;
  }

  .footer-links a {
    color: var(--text-muted);
    text-decoration: none;
    margin: 0 8px;
  }

  .footer-links a:hover {
    color: var(--primary);
  }

  /* Responsive */
  @media (max-width: 968px) {
    .hero .container {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .logo {
      justify-content: center;
    }

    .description {
      margin: 0 auto 32px;
    }

    .cta-buttons {
      justify-content: center;
    }

    .hero-image {
      display: none;
    }

    .features-grid,
    .download-grid,
    .contact-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
